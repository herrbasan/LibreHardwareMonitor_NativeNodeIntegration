{
  "name": "libremon-cli",
  "version": "0.1.0",
  "description": "Standalone CLI for LibreHardwareMonitor - outputs hardware sensor data as JSON",
  "scripts": {
    "build": "npm run build:lhm && npm run build:cli",
    "build:lhm": "node scripts/build-lhm.js",
    "build:cli": "dotnet publish managed/LibreMonCLI/LibreMonCLI.csproj -c Release -o dist",
    "clean": "rimraf deps/LibreHardwareMonitor && rimraf managed/*/bin managed/*/obj && rimraf dist",
    "rebuild": "npm run clean && npm run build",
    "test": "node test/compare-web-vs-native.js"
  },
  "keywords": [
    "hardware",
    "monitoring",
    "sensors",
    "cpu",
    "gpu",
    "temperature",
    "LibreHardwareMonitor",
    "cli",
    "command-line"
  ],
  "author": "herrbasan",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/herrbasan/LibreHardwareMonitor_NativeNodeIntegration.git"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "os": [
    "win32"
  ],
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
